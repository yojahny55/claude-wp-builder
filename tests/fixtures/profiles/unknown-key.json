{
  "name": "Unknown key",
  "description": "A plugin entry carries a key validateProfile does not recognise",
  "plugins": [
    { "slug": "secure-custom-fields", "required": true },
    { "slug": "contact-form-7", "required": false, "version": "5.8" }
  ]
}
