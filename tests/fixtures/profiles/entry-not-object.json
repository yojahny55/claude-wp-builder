{
  "name": "Entry not object",
  "description": "A plugins array item is a bare string instead of an object",
  "plugins": [
    { "slug": "secure-custom-fields", "required": true },
    "contact-form-7"
  ]
}
