{
  "name": "Unresolved",
  "description": "Requires a plugin the profile never lists",
  "plugins": [
    { "slug": "secure-custom-fields", "required": true },
    { "slug": "woocommerce-gateway-stripe", "required": false, "requires": ["woocommerce"] }
  ]
}
