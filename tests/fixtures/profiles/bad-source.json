{
  "name": "Bad source",
  "description": "A plugin's source is outside the wordpress.org/supplied enum",
  "plugins": [
    { "slug": "secure-custom-fields", "required": true },
    { "slug": "custom-plugin", "required": false, "source": "npm" }
  ]
}
