{
  "name": "Slug not canonical",
  "description": "slug is not already lowercase and trimmed",
  "plugins": [
    { "slug": "secure-custom-fields", "required": true },
    { "slug": "Contact-Form-7", "required": false }
  ]
}
